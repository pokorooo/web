{
  "name": "web-apps-root",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm --prefix baseball_order ci && npm --prefix baseball_order run build"
  }
}

